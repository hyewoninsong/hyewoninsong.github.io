---
project: "super-math-quiz"
date: "2026-04-14"
summary: "subtypeLabel 추적·플레이어 이름·확장형 서브타입 통계 기능 추가 후 문서 3개 업데이트 및 단위 테스트 9개 추가"
tags: ["docs", "tests"]
commits: 11
docsUpdated: 3
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- `feat: add App Store screenshots (ko/en-US, 12-13 screens each)`
- `fix: call setupSnapshot(app) in UITest setUp so screenshots are saved`
- `docs: add App Store promotional copy and screenshot candidate list`
- `feat: configure fastlane screenshots lane for multi-group UITest`
- `feat: add App Store screenshot UITest classes (groups A-E + video)`
- `feat: add accessibility identifiers to SwiftUI views for UITest`
- `feat: add SampleDataLoader for App Store screenshot preloading`
- `feat: expandable subtype stats and inline badge in HistoryView`
- `feat: track subtypeLabel from generator through history entry`
- `feat: add per-player history with name input on home screen`
- `docs: reorganize docs into policy-defined folders`

## 문서 감사 결과

- **`docs/modules/navigation.md` 업데이트**
  - `HomeView` 하단 바에 플레이어 이름 `TextField`(`home_playerNameField`) 추가 설명
  - `HomeHistoryView` (홈에서 열리는 기록 시트) 항목 추가
  - `currentPlayerName` UserDefaults 저장 동작 설명 추가
  - Mermaid 다이어그램에 `HomeHistoryView` 노드 추가

- **`docs/modules/ui-components.md` 업데이트**
  - `HistoryView` 설명을 세션 내 시트 vs. 홈 기록 시트 두 컨텍스트로 분리
  - 확장형 서브타입 행(`expandedOps` 상태, chevron 방향 규칙) 설명 추가
  - `subtypeLabel` 뱃지 인라인 배치 규칙 설명 추가
  - 접근성 식별자 명명 규칙 표(`home_*`, `pad_*`, `quiz_*`, `feedback_*`, `history_*`, `subtype_*`, `settings_*`) 추가

- **`docs/modules/problem-generation.md` 업데이트**
  - `subtypeLabel` 전파 경로(`ProblemGenerator` → `MathProblem` → `QuizHistoryEntry`) 설명 추가
  - 구구단 레이블 형식(`"구구단 N단"`, `"구구단 나눗셈 N단"`) 명시

## 테스트 감사 결과

- **`mathmasterTests/mathmasterTests.swift` 업데이트** — 단위 테스트 9개 추가, 모두 통과

  `MathProblemSubtypeLabelTests` (3개):
  - `defaultInitHasNilSubtypeLabel` — 기존 init은 `subtypeLabel == nil`
  - `subtypeLabelInitPreservesLabel` — 새 init이 레이블 보존
  - `subtypeLabelNilInitProducesNil` — 명시적 nil 전달 시 nil 유지

  `HistoryEntrySubtypeLabelTests` (6개):
  - `subtypeLabelRoundtrips` — encode → decode 후 레이블 보존
  - `nilSubtypeLabelRoundtrips` — nil 레이블 encode/decode
  - `legacyEntryWithoutSubtypeLabelDecodesAsNil` — `subtypeLabel` 키 없는 구버전 JSON → nil
  - `playerNameDefaultsToNaForLegacyEntries` — `playerName` 키 없는 구버전 JSON → `"나"`
  - `playerNamePreservedAfterRoundtrip` — encode → decode 후 playerName 보존

  기존 `MigrationTests` 3개는 감사 이전부터 실패 상태 (잘못된 메서드 시그니처 호출) — 이번 감사 범위 밖.

## 요약

11개 커밋 중 핵심 변경은 `subtypeLabel` 데이터 흐름 추가, 플레이어별 기록 분리, 확장형 서브타입 통계 UI, 접근성 식별자 전면 도입, App Store 스크린샷 자동화 인프라다. 문서 3개를 실제 코드 상태에 맞게 갱신하고, 이전에 커버리지가 없던 `MathProblem.subtypeLabel`·`QuizHistoryEntry` 직렬화 경로를 검증하는 단위 테스트 9개를 추가했다. 신규 테스트 9개 전부 통과.