---
project: "super-font"
date: "2026-04-10"
summary: "WizardStep 5번째 케이스 누락 테스트 수정 및 docs/02-user-flow.md 업데이트"
tags: ["docs", "tests"]
commits: 12
docsUpdated: 1
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- `fix: sync installed families after refresh, make all canReinstall fonts selectable`
- `improve: remove fileSelection step from StepIndicator, renumber 1-4`
- `fix: defer resetWizardState to onDismiss to prevent index-out-of-bounds crash`
- `staging: merge fix/file-add-tab-bugs`
- `fix: font count plural and TTF display name in FileAddTab`
- `fix: open mobileconfig URL directly in Safari to show download popup immediately`
- `improve: remove selection checkmarks from PreviewFonts step`
- `improve: use short profile title, put font list in description only`
- `improve: redesign PreviewFontFamilyGroupRow to match FontFamilyGroupRow style`
- `feat: add selected-only filter chip to GoogleFonts and MyFonts tabs`
- `fix: run local server on background queue to fix popup delay`
- `Merge pull request #12 from hyewoninsong/fix/google-fonts-observedobject-reactivity`

## 문서 감사 결과

- **`docs/02-user-flow.md` 업데이트** — WizardStep enum 코드 블록에 `installComplete = 5` 케이스 추가, StepIndicator가 `fileSelection`을 숨기고 나머지 4단계(1-4)만 표시한다는 설명 추가, 단계별 설명 테이블을 로컬라이즈 키 기준으로 정정

## 테스트 감사 결과

- **`FontManagerTests/FontManagerTests.swift` — WizardStepTests 수정**
  - `testWizardStepOrder`: `steps.count == 4` → `steps.count == 5` (실제 enum은 5 케이스)
  - `testWizardStepNavigation`: `installGuide.next == .installComplete`, `installComplete.next == nil` 추가
  - `testWizardStepPrevious`: `installComplete.previous == .installGuide` 추가
  - `testWizardStepIsLast`: `installGuide.isLast == true` → `installComplete.isLast == true`로 수정
  - **`testStepIndicatorVisibleSteps` 신규 추가**: `fileSelection` 제외 후 4개 visible step 검증

## 요약

`WizardStep` enum에는 5개 케이스(`fileSelection`~`installComplete`)가 있으나, 기존 테스트는 4개를 기대하고 있었고 `isLast` 체크도 잘못된 step(`installGuide`)을 가리키고 있었다. 또한 StepIndicator가 `fileSelection`을 숨기는 로직이 문서에 반영되지 않았다. 테스트 6개 수정·추가 후 전체 통과 확인. `docs/02-user-flow.md` WizardStep 섹션도 현재 코드와 일치하도록 갱신했다.