---
project: "super-font"
date: "2026-04-13"
summary: "설치 가이드 문서 및 유저 플로우 문서를 현재 코드 기준으로 업데이트, maxReachedStep 테스트 추가"
tags: ["docs", "tests"]
commits: 6
docsUpdated: 2
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- `c66fa71` fix: show next button when returning to previous wizard step
- `0199476` fix: readable filter chips in dark mode
- `265315b` fix: clean up My Fonts tab UI
- `83a9ec9` fix: localize InstallGuideView strings, fix screenshot, rename wizard steps
- `8b93967` fix: use profile-based install check in FontPreviewRow
- `b7f24cd` feat: add real screenshots to install guide steps

## 문서 감사 결과

### 업데이트된 파일

**`docs/06-install-guide.md`** — 전면 재작성:
- 이전: InstallGuideView 내부에 6단계 TabView가 직접 존재하는 구조로 기술
- 이후: 실제 코드에 맞게 수정 — 메인 화면(아이콘 + 제목 + "가이드 보기" 버튼)과 `InstallGuideSheetView` 시트 분리 구조, 갱신된 6단계 내용(새 5단계 `unsignedWarning` 포함), 실제 스크린샷(`screenshotName`) 필드, scenePhase 기반 자동 진행 로직, 조건부 "다음" 버튼 동작 문서화

**`docs/02-user-flow.md`** — 두 곳 수정:
- Flow A의 Step 4 설명 수정: "완료 탭 → completeInstallation()" → 실제 동작인 "가이드 보기 시트 → 설정 앱 → 자동 감지 후 Step 5 진행"으로 업데이트. InstallCompleteView(Step 5)에서 completeInstallation() 호출하는 흐름도 명시
- `maxReachedStep` 섹션 신규 추가: 이전 단계 복귀 시 "다음" 버튼 표시 로직, 리셋 조건 문서화 (commit `c66fa71` 반영)

## 테스트 감사 결과

### 추가된 테스트

**`FontManagerTests/FontManagerTests.swift`** — `AppStateMaxReachedStepTests` 섹션 추가:
- `testMaxReachedStepAdvancesOnNextStep`: `goToNextStep()` 호출 시 `maxReachedStep`이 앞으로 갱신되는지 확인
- `testMaxReachedStepDoesNotRegressOnPreviousStep`: `goToPreviousStep()` 호출 시 `maxReachedStep`이 퇴보하지 않는지 확인
- `testMaxReachedStepNotOverwrittenWhenReturningToEarlierStep`: 이미 지나간 단계로 다시 넘어갈 때 `maxReachedStep`이 덮어씌워지지 않는지 확인
- `testMaxReachedStepResetsOnDismiss`: `dismissWizard()` 시 `maxReachedStep`이 `.fileSelection`으로 리셋되는지 확인

## 요약

오늘 6개 커밋 중 가장 큰 구조 변화는 `InstallGuideView` 리팩터링(6단계 TabView를 별도 시트로 분리, 실제 스크린샷 추가, 로컬라이징)과 이전 단계 복귀 시 "다음" 버튼 표시(`maxReachedStep`) 추가였다. `docs/06-install-guide.md`가 이전 구조 기준으로 작성되어 있어 현재 코드와 크게 달랐으므로 전면 재작성했다. `docs/02-user-flow.md`는 Step 4/5 흐름과 `maxReachedStep` 설명이 누락되어 있어 보완했다. 또한 `maxReachedStep` 로직에 대한 유닛 테스트 4개를 추가해 이전 단계 복귀 시 "다음" 버튼 표시 동작을 검증한다.