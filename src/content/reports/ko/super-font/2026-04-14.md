---
project: "super-font"
date: "2026-04-14"
summary: "InstallGuide 7단계 문서 갱신 및 allFontsList 삽입 순서 회귀 테스트 추가"
tags: ["docs", "tests"]
commits: 7
docsUpdated: 1
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- `334f3d3` docs: add promotional assets for SuperFont
- `ec66c2a` docs: add PreInstallGuide step rules and allFontsList sort principle
- `7695ec1` fix: preserve insertion order in allFontsList for correct 추가순 sort
- `2b8487d` improve: update pre-install guide steps and add reinstall-tap step
- `701ff91` Merge pull request #19 from hyewoninsong/fix/guide-sheet-icon-inline
- `65f724b` fix: move step icon inline with title to prevent description truncation
- `e3234c9` docs: reorganize docs folder per documentation policy

## 문서 감사 결과

- **`docs/product-specs/install-guide.md`** 갱신: InstallGuideView가 7단계로 늘어났으나 문서에는 여전히 6단계로 기록되어 있었음. Step 5 (`reinstallTapTitle` / `hand.tap.fill`, 암호 입력 후 '설치' 재탭)를 표에 추가하고, 기존 6번 경고 단계를 7번으로 밀어 올바르게 반영.

## 테스트 감사 결과

- **`FontManagerTests/FontManagerTests.swift`** — `AllFontsListInsertionOrderTests` 구조체 추가:
  - `allFontsListPreservesInsertionOrder`: `installedProfiles`를 Z→A 순으로 설정한 뒤 `refreshAllFontsList()` 후 `allFontsList`가 알파벳 정렬이 아닌 삽입 순서를 유지하는지 검증 (커밋 `7695ec1` 버그 회귀 방지).
  - `allFontsListDeduplicatesPreservingFirstOccurrence`: 여러 프로파일에 중복 폰트가 있을 때 첫 등장 위치만 보존하는지 검증.

## 요약

최근 24시간 내 7개 커밋이 있었다. 주요 변경은 두 가지다. ① `allFontsList`의 알파벳 정렬 제거(`7695ec1`)로 '추가순' 정렬이 실제 삽입 순서를 따르도록 수정되었으나 테스트가 없었으므로 회귀 테스트 2개를 추가했다. ② `InstallGuideView`에 암호 입력 후 '설치 재탭' 단계가 추가되어 총 7단계가 되었으나 스펙 문서(`install-guide.md`)가 6단계로 남아 있었으므로 이를 갱신했다. `PreInstallGuideSheet` Step 3 아이콘·설명 변경 및 `StepIndicator` 레이아웃 수정은 기존 문서 범위를 벗어나지 않아 별도 문서 수정 불필요.