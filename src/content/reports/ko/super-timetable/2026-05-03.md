---
project: "super-timetable"
date: "2026-05-03"
summary: "Spring 애니메이션 마이그레이션(PR-D1/D2)과 Liquid Glass 추가 적용에 맞춰 모듈 가이드 2개 + 프로덕트 스펙 3개를 최신화"
tags: ["docs", "specs"]
commits: 10
docsUpdated: 2
specsUpdated: 3
testsUpdated: 0
---

## 오늘의 변경 사항

- `a3b690b` fix: DisplaySettings 시트 진짜 반투명 — modifier 모두 제거 + UnifiedScheduleEditSheet dead code 제거
- `5dab8bb` fix: DisplaySettings 시트 modifier 순서 — UnifiedScheduleEditSheet 와 정렬
- `17674ed` refactor: Content rules — "OK" 단독 버튼 → 의미 verb (Add / Rename) 교체 (#80)
- `cb10e43` docs: Radii ramp 정렬 — off-ramp 보존 정책 + 사용처 카탈로그 (#79)
- `d4ac826` feat: Liquid Glass 추가 적용 — share popup / iPad sub-toolbar / grid card (#78)
- `6388c4a` refactor: Spring animation PR-D2 — `.animation` modifier ease → spring 18곳 (#77)
- `40378c6` refactor: Spring animation PR-D1 — withAnimation ease → spring 34곳 (#76)
- `69dd47d` docs: UnifiedScheduleEditSheet 삭제 후속 — docs/test references 정리 (#75)
- `dcc252b` docs: Dynamic Type PR-C4 — ≤10pt 보존 정책 명시 (#74)
- `cda7835` refactor: Dynamic Type PR-C2 — design 인자 매핑 11곳 + off-ramp 보존 정책 (#73)

## 문서 감사 결과

- `docs/design-docs/modules/ipad.md` — iPad 서브 툴바 배경 설명을 `.ultraThinMaterial` → `.liquidGlass(in: RoundedRectangle(cornerRadius: 12, style: .continuous))` 로 갱신 (PR #78 반영).
- `docs/design-docs/modules/sheets.md` — `InfoQuickEditSheet` detent 전환 곡선을 `.easeInOut(duration: 0.25)` → `.spring(response: 0.35, dampingFraction: 0.8)` 로 갱신 (PR #77 반영).
- `docs/product-specs/13-animation-spec.md` — PR-D1/D2 의 spring 매핑표·off-ramp(`TimetableGridView.swift:3451 easeOut(0.9)`)·정책 (새 코드 ease 금지) 으로 전면 재작성. 기존 "기본 전환 easeInOut 0.2초" 형식의 stale 표 제거.
- `docs/product-specs/11-batch-edit.md` — 요일·시각 비활성화 전환 애니메이션을 `.easeInOut(0.2)` → `.spring(response: 0.3, dampingFraction: 0.85)` 로 갱신.
- `docs/product-specs/07-tablet-layout.md` — 편집 패널 슬라이드 인 곡선을 `0.25초, easeInOut` → `spring(response: 0.35, dampingFraction: 0.8)` 로 갱신.
- `docs/design-docs/decision-log.md` 의 과거 항목 (line 1116, 1410) 에 남아있는 `easeInOut` 표기는 **역사 기록**이므로 의도적으로 보존.
- `docs/design-docs/modules/export-share.md` 의 `TimetableCarouselSheet.swift` 항목은 working tree 의 미커밋 삭제 상태이므로 이번 감사에서 손대지 않음 (다음 커밋 시점에 재감사).

## 테스트 감사 결과

- 모든 테스트가 최신 상태. 최근 24시간 변경은 모두 (1) 시각 chrome 정책, (2) 내부 애니메이션 곡선, (3) 다국어 라벨 키 재사용 (`OK` → `Add`/`Rename`) 류여서 신규 기능 표면이 없음.
- "OK" → `Add`/`Rename` 변경은 `TimetableListUITests.swift` 가 `alert.buttons.element(boundBy: 1)` 인덱스 기반으로 alert 확인 버튼을 누르므로 라벨 변경의 영향 없음 — 추가 테스트 불필요.
- Liquid Glass / spring 마이그레이션은 사용자에게 노출된 기능 동작 변경이 아니므로 신규 단위/UI 테스트 대상 아님 (시각 회귀는 별도 스냅샷 캡처 파이프라인 영역).
- `69dd47d` 가 이미 `NotificationUITests.swift` 의 `UnifiedScheduleEditSheet` 잔재를 정리해 둠 — 추가 정리할 곳 없음.

## 요약

오늘 감사의 핵심은 PR-D1/D2 의 spring 마이그레이션 (52곳) 과 PR #78 의 Liquid Glass 추가 적용을 spec/모듈 가이드에 반영한 것. 특히 `13-animation-spec.md` 가 옛 `easeInOut` 표 그대로였어서 새 정책과 정면충돌하던 문제를 해소했고, 모듈 가이드의 chrome 표면 설명도 코드 현실과 정렬했다. 신규 기능이 추가된 PR 가 없어 테스트 갱신은 없음.