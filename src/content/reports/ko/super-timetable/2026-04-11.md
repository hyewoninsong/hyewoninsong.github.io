---
project: "super-timetable"
date: "2026-04-11"
summary: "no-color UI 및 인라인 프리셋 피커 기능 추가에 따른 문서·스펙·테스트 감사 및 수정"
tags: ["docs", "specs", "tests"]
commits: 4
docsUpdated: 1
specsUpdated: 1
testsUpdated: 1
---

## 오늘의 변경 사항

- `9f00524` fix: no-color UI, preset count, and legend icon in schedule editor
- `2ab5cf7` feat: add collapsible preset legend to timetable edit mode
- `e2faf7b` feat: add orphan preset cleanup tests and no-color/inline picker specs
- `c9e32f3` improve: sync build number to 62

## 문서 감사 결과

**`docs/modules/presets-themes.md`** — 3가지 코드-문서 불일치 수정:
- `colorForIndex` 폴백 색상: `Color(.systemFill)` → `Color(.systemGray5)` 정정
- Done 버튼 비활성 조건 목록 확장 (5가지 조건 모두 반영: `!isNewSchedule && !hasChanges`, `timeWarningMessage != nil` 추가)
- 인라인 프리셋 피커 토글 아이콘 수정: `chevron.right` → `list.bullet.rectangle` (닫힘) / `clock` (열림); 범례 확장 시 `xmark` 아이콘 상태 추가

## 스펙 감사 결과

**`specs/05-presets-themes.md`** — 2가지 갭 보완:
- Done 버튼 비활성 조건 완성 (문서와 동일한 누락 항목 추가)
- 인라인 피커 ASCII 다이어그램 및 SF Symbol 명칭 정정 (`>` / `⟳` → `list.bullet.rectangle` / `clock`)

## 테스트 감사 결과

**`TimetableTests/TimetableStoreTests.swift`** — 2개 테스트 추가:
- `unlinkAndRemovePreset()`: preset 삭제 시 연결된 스케줄은 보존하고 `presetID`만 nil로 설정하는 동작 검증 (기존 `removePreset`과 구분)
- `duplicateTimetableWithPresets()`: 타임테이블 복제 시 preset ID가 딥카피되고, 복제된 스케줄의 `presetID`가 원본이 아닌 복제된 preset을 가리키는지 검증

## 요약

최근 24시간 내 4개 커밋이 있었으며, no-color UI 및 인라인 프리셋 피커·범례 기능이 추가됐다. 코드와 문서 사이 3곳의 불일치(폴백 색상, 버튼 조건, 아이콘 이름)를 수정했고, 스펙 파일도 동기화했다. 또한 `unlinkAndRemovePreset`과 타임테이블 복제 관련 테스트 2개가 누락된 것을 발견해 추가했다.