---
project: "super-timetable"
date: "2026-09-24"
summary: "스타일 팝오버 #513 변경을 문서·사양 3개에 반영하고, compact 편집 시트 글자색 환경값 상속 계약 테스트 추가"
tags: ["docs", "specs", "tests"]
commits: 4
docsUpdated: 1
specsUpdated: 2
testsUpdated: 1
---

## 오늘의 변경 사항

- `618a82d1` feat(style-sheet): 스타일 팝오버 네 가지 변경 (#513)
  - 헤더 글자색이 시간표의 글자색 설정을 따름
  - 롱프레스하면 편집 모드로 들어감
  - "사용 중" 원 안에도 제목 표시
  - 에디터의 `<` 버튼을 없애고, 바깥을 탭하면 ✓ 와 같게 처리
- `ab4d9f9f` test(palette): 계약 테스트를 새 팔레트에 맞춤 (#511)
- `4e2af0a8`, `3e28b8fe` 결정 로그 인덱스 자동 갱신 (Action)

## 문서 감사 결과

- `docs/design-docs/modules/sheets.md`
  - 에디터 좌상단 `<`(`color_editor_back`)가 없어진 것 반영
  - 바깥 탭으로 닫으면 `commitEditorOnDismiss` 가 색을 추가·저장함
  - 되돌리기는 하단 "삭제"에만 남음
  - "사용 중" 원 안 제목 규격 추가
  - `headerForeground` 를 `titleForeground(textColorMode)` 로 정정
- `docs/product-specs/05-presets-themes.md` (사양)
  - 색 순서 변경: 롱프레스는 편집 모드 진입, 편집 모드에서는 그냥 끌어서 옮김
  - 글자색 미리보기 자리가 모두 시간표 글자색 모드를 따른다고 명시
- `docs/product-specs/04-schedule-editing.md` (사양)
  - 헤더 글자색이 시간표 글자색 모드를 따르도록 정정
  - "초안 + 확인, 취소는 X" 라는 옛 문장을 "즉시 반영, 취소 없음, ✓ 와 바깥 탭은 같은 결과" 로 수정

## 테스트 감사 결과

- `TimetableTests/ColorPickerSheetContractTests.swift`: `compactQuickEditSheet_inheritsTextColorModeFromGrid` 테스트 추가
  - 그리드의 `.environment(\.scheduleTextColorMode, …)` 가 편집 시트 `.sheet(item: $infoQuickEditScheduleID` 보다 바깥에 걸려 있는지 확인함
  - #513 결정 문서는 compact 편집 시트가 이 값을 물려받는다는 전제에 기대는데, 이를 확인하는 테스트가 없었음
  - 네 가지 변경의 나머지 동작은 #513 에서 이미 계약 테스트로 다룸
- #511 은 테스트만 바꾼 커밋이라 따로 감사할 것이 없음
- `ColorPickerSheetContractTests` 를 실행했고 TEST SUCCEEDED

## 요약

최근 24시간 커밋은 4개이고, 실제 변경은 #513 과 #511 두 건이다. #513 이후에도 모듈 문서 1개와 사양서 2개에 옛 동작이 남아 있어서 현재 코드에 맞게 고쳤다. 남아 있던 옛 동작은 에디터 `<` 버튼, 롱프레스로 바로 들어 올리기, 자동 판정 글자색이었다. 빠져 있던 compact 환경값 상속 계약 테스트도 하나 추가했다. 소스 코드는 건드리지 않았다.

PR: https://github.com/hyewoninsong/timetable_ios/pull/514