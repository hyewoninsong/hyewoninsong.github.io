---
project: "super-timetable"
date: "2026-09-15"
summary: "지난 24시간 40개 커밋 감사 — 문서/스펙은 이미 코드와 동기화됨, 인쇄 옵션 패널 접기 왕복 테스트 공백 1건 보강"
tags: ["tests"]
commits: 40
docsUpdated: 0
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

지난 24시간 내 40개 커밋 (`docs: 결정 로그 인덱스 자동 갱신 [skip ci]` 20개 포함, 실질 변경 커밋 20개):

- feat(alarm): 잠금 화면 카드 재구성 (#451)
- fix(print): 프리뷰 전체화면 철회 — 옵션 패널 접기로 복귀 (#450)
- fix(print): 숫자 입력 필드 서식 되쓰기 문제 — NumericTextField 신설 (#447)
- feat(print): 프리뷰 전체화면 — Safari 방식 (#448, 이후 #450에서 철회)
- fix(print): 한 줄 잘림 판정 — CTLine 글리프 잉크 실측 (#449)
- feat(print): 옵션 패널 접기 — 손잡이 한 번으로 프리뷰 확대 (#446)
- fix(print): 결과 캡션 잘림 개수 오차 (#445)
- feat(print): 일정 글자 크기 균일 규칙 (#444)
- refactor(settings): 일정 글자 색상 행 이름 정리 (#443)
- feat(widgets): 알람 카드 아이콘 — 앱 아이콘 그대로 (#440)
- feat(print): 크기 배지 위치 + 처방 문구 (#442)
- refactor(alarms): 시간표 단위 알람 끄기 폐기 (#441)
- fix(alarm): 앱 실행 중 알람 "열기" 버튼 이동 문제 (#439)
- feat(widgets): 잠금 화면 카드 지면 색 복원 (#436)
- feat(settings): 표시 방식 시트 분리 (#437)
- feat(print): 인쇄 설정 요약 행 (#438)
- feat(alarm): 알람 배지 — 잠긴 그리드·목록 카드 (#435)
- feat(grid): 알람 표시 — 편집 모드 배지로 이동 (#434)
- fix(alarm): 취소 대기열 순환 차단 — AlarmKit cancel 함정 (#433)
- fix(settings): 전체 알림 초기화 — 단독 그룹 분리 (#432)

## 문서 감사 결과

모든 문서가 최신 상태. 서브에이전트가 `docs/design-docs/modules/`, `docs/product-specs/`, 루트 `CLAUDE.md` 를 `git diff` 로 전수 대조한 결과, 인쇄 프리뷰 전체화면↔접기 왕복(#446→#448→#450), 표시 설정 시트 분리(#437), 일정 글자 색 per-timetable 정리(#443), 알람 잠금 화면 카드 재구성(#436/#451), 시간표 단위 알람 끄기 폐기(#441) 모두 해당 커밋 안에서 문서가 함께 갱신되어 현재 코드와 일치함을 확인. 수정 불필요.

**참고 (이번 감사 범위 밖, 기존부터 존재하던 불일치)**: `docs/product-specs/01-data-model.md` 14행이 2026-05-28에 제거된 테마 시스템("11종 standard 색상 테마")을 여전히 언급. 이번 24시간 변경과 무관해 손대지 않음 — 별도 후속 조치 권장.

## 테스트 감사 결과

- **추가**: `TimetableTests/PrintPanelCollapseSourceTests.swift` — 인쇄 옵션 패널이 같은 날 세 번(접기→전체화면→접기 복귀) 뒤집혔는데도 테스트 공백이었던 부분을 보강. `printOptionsPanelCollapsed` AppStorage 배선, `print_panel_collapse_toggle` 식별자 존재, `PrintPreviewFullScreen.swift` 가 삭제된 채로 유지되는지를 소스 계약 테스트로 고정. iPhone 17 시뮬레이터 빌드·실행 확인 (3건 통과).
- 나머지 실질 변경(#449 CTLine 잉크 실측, #433 AlarmKit cancel 함정, #447 NumericTextField, #439 포그라운드 알람 열기, #451 알람 카드)은 이미 기존 테스트(`PrintUniformTextSizingTests`, `AlarmCancelConvergenceTests`, `NumericTextFieldSourceTests`, `AlarmStopAndOpenTests`, `ScheduleAlarmServiceTests`/`AlarmCardAppIconAssetTests`)로 커버됨. #441 알람 토글 폐기는 `TimetableAlarmFindTests.swift` 의 부정 단언(토글·메서드가 실제로 사라졌는지) 테스트로 정확히 반영되어 있어 손대지 않음.

## 요약

지난 24시간 인쇄·알람·설정·위젯 기능에 걸친 20개 실질 커밋을 전수 대조한 결과 문서/스펙은 이미 코드와 함께 갱신되어 있었고, 인쇄 옵션 패널 접기/전체화면 왕복 로직에만 테스트 공백이 있어 소스 계약 테스트 1개를 추가했다. 소스 코드는 수정하지 않았으며, 변경 사항은 PR #455로 올렸다: https://github.com/hyewoninsong/timetable_ios/pull/455