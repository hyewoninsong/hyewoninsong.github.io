---
project: "super-timetable"
date: "2026-04-15"
summary: "최근 24시간 6개 커밋 — updatedAt 필드, 시간표 목록 레이아웃, 배치 업데이트 UI 변경에 맞게 문서 5개·테스트 3개 업데이트"
tags: ["docs", "specs", "tests"]
commits: 6
docsUpdated: 5
specsUpdated: 0
testsUpdated: 3
---

## 오늘의 변경 사항

- `0fe48b1` docs: update module docs + specs for preset removal; bump build version 95
- `9038d11` test: update unit tests for preset removal + updatedAt field
- `7a5940b` feat: localize new UI strings (en/ko/ja/zh-Hans)
- `6bec391` refactor: timetable list → horizontal paging + card layout
- `5120657` improve: sheet UI — batch update inline row, color picker, validation cues
- `0042886` feat: add updatedAt to Schedule + pre-fill new schedule from last modified

## 문서 감사 결과

다음 5개 파일을 업데이트했습니다:

1. **`docs/product-specs/01-data-model.md`** — Schedule 필드 표에 `updatedAt: Date` 추가, JSON 예시에 `updatedAt` 키 추가
2. **`docs/product-specs/04-schedule-editing.md`** — 배치 업데이트 섹션을 "확인 다이얼로그" → "인라인 버튼 행" 방식으로 전면 재작성 (트리거 조건, 행 구성, 버튼 동작 반영)
3. **`docs/product-specs/06-timetable-list.md`** — Phone 섹션을 가로 페이징 + `TimetablePageCard` 구조로 재작성 (페이지 인디케이터, 2단계 삭제 확인, `NewTimetableSheet` 통합 복제 플로우 반영)
4. **`docs/design-docs/modules/sheets.md`** — 배치 업데이트 인라인 버튼 행 + `ColorPickerPopoverContent` 10열 flat LazyVGrid 방식 추가
5. **`docs/design-docs/modules/store.md`** — `addSchedule`/`updateSchedule`의 `updatedAt` 자동 스탬프 섹션 추가

## 테스트 감사 결과

다음 3개 파일에 테스트를 추가했습니다:

1. **`TimetableTests/TimetableModelTests.swift`** — `Schedule` 기본 생성 시 `updatedAt` 검증, `updatedAt` JSON 라운드트립 보존 테스트 추가
2. **`TimetableTests/TimetableStoreTests.swift`** — `addSchedule` 및 `updateSchedule` 호출 시 Store가 `updatedAt`을 현재 시각으로 스탬프하는지 검증하는 테스트 2개 추가
3. **`TimetableTests/CodableMigrationTests.swift`** — `updatedAt` 없는 구버전 데이터 디코딩 시 에포크(1970-01-01) fallback 검증, `updatedAt` 포함 라운드트립 검증 테스트 2개 추가

## 요약

최근 24시간 내 6개 커밋으로 `Schedule.updatedAt` 필드 추가, 시간표 목록의 가로 페이징 레이아웃 전환, 배치 업데이트 UI를 확인 다이얼로그에서 인라인 버튼으로 변경, 컬러 피커를 TabView에서 flat LazyVGrid로 변경하는 작업이 이루어졌습니다. 이에 맞춰 스펙 및 모듈 문서 5개를 현행화하고, 누락된 `updatedAt` 관련 테스트 6개를 3개 파일에 추가했습니다.