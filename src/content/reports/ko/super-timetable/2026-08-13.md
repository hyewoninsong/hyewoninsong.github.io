---
project: "super-timetable"
date: "2026-08-13"
summary: "end pill 오클루전 복귀 규칙을 gestures.md에 반영하고 제스처 스펙의 깨진 테이블을 복구"
tags: ["docs", "specs"]
commits: 2
docsUpdated: 1
specsUpdated: 1
testsUpdated: 0
---

## 오늘의 변경 사항

- `0ce91f7` fix: end pill returns to natural spot when blocked resize overshoots — 하단 리사이즈/생성 드래그가 다른 일정·그리드 경계에 막힌 상태에서 손가락이 끝선보다 `GridLayout.endPillReleaseClearance`(60pt) 이상 아래로 벗어나면 종료 시각 pill 이 자연 위치로 복귀
- `d116e17` fix: color popover page indicator overlap and sheet detent jump (#113) — TabView 시스템 페이지 인디케이터를 커스텀 점 행으로 교체, 팝오버 열림이 시트 detent 를 강제 확장하지 않도록 `onEditorToggle` 콜백 도입

## 문서 감사 결과

- `docs/design-docs/modules/gestures.md` — "Pill 오클루전 가드" 섹션이 컬럼 X 검사만 기술하고 있어 실제 코드와 불일치. `endPillOccluded` 의사코드에 시작선 Y 상한 규칙과 새 `endPillReleaseClearance` 복귀 규칙을 반영하고, 상수 산정 근거(60pt = pill 하단 + 손끝 반경 + 여유)와 프리뷰 경로(clamped `endMin`) 동일 적용, 계약 테스트 위치를 추가함
- `docs/product-specs/03-gestures.md` — 이전 세션 편집이 제스처 테이블 중간에 불릿을 끼워 넣어 마크다운 테이블이 두 동강 난 상태였음. 테이블을 복구하고 pill 자연 위치 복귀 동작을 사용자 관점 불릿으로 추가함
- `d116e17` 관련 문서(`sheets.md`, `Sheets/CLAUDE.md`, decision-log)는 해당 커밋에 이미 포함되어 있고 코드(`QuickEditSheets.swift`, `ColorPickerView.swift`)와 일치함을 확인 — 추가 수정 불필요

## 테스트 감사 결과

- 모든 테스트가 최신 상태 — 추가 작성 없음
- `0ce91f7`: `EndPillOcclusionG3Tests` 에 QA-G3-7(클리어런스 이내 → 오클루전 유지) / QA-G3-8(클리어런스 초과 → 자연 위치 복귀) 시나리오와 소스 감사 regex 가 같은 커밋에 포함됨
- `d116e17`: 워킹 트리의 `SheetUIPolishG1Tests` QA-G1-13 갱신본이 새 계약(`isColorEditorExpanded` 가 detent 를 구동, `showingColorPicker` 참조 금지, 팝오버 닫힘 시 플래그 리셋)을 커버하며, regex 대상 코드가 실제 소스와 일치함을 확인

## 요약

최근 24시간 커밋 2건을 감사했다. 두 커밋 모두 decision-log 와 계약 테스트는 자체적으로 갖추고 있었으나, end pill 복귀 커밋(`0ce91f7`)이 모듈 문서를 갱신하지 않아 `gestures.md` 오클루전 섹션이 코드와 어긋나 있었고 이를 동기화했다. 추가로 제스처 스펙의 깨진 테이블 포맷을 복구했다. 소스 코드는 일절 수정하지 않았고, 테스트는 이미 충분해 신규 작성이 필요 없었다.