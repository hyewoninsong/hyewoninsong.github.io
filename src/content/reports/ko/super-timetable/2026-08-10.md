---
project: "super-timetable"
date: "2026-08-10"
summary: "인쇄 신기능(용지/배율/N-up/프린터 직행) 반영해 모듈 문서 2건·스펙 1건 갱신, 테스트는 커밋에 이미 포함되어 최신"
tags: ["docs", "specs"]
commits: 3
docsUpdated: 2
specsUpdated: 1
testsUpdated: 0
---

## 오늘의 변경 사항

- `c266cb2` feat: print paper size, scale modes, N-up copies, printer direct print — `PrintService.swift` 대규모 확장(+335줄), `PrinterStore.swift` 신규, `PrintOptionsSheet.swift` 개편, 5로케일 20키 추가, 테스트 3파일 추가/갱신
- `77a9b90` docs: iPad App Store 세트 보류 표기 (문서만)
- `1277795` feat: 16샷 App Store 캡처 플랜 + 프리뷰 비디오 — 소스 변경은 `QuickEditSheets.swift` 접근성 식별자 추가뿐(테스트 관측성 목적, UI 무변경, decision-log 에 기록 완료)

## 문서 감사 결과

- `docs/design-docs/modules/export-share.md` — 인쇄 섹션이 구버전(방향/색상/제목만) 그대로였음. 파일 표에 `PrinterStore.swift` 추가, `PrintService`/`PrintOptionsSheet` 역할 갱신, 인쇄 섹션을 신기능 전체(PrintPaper 프리셋+커스텀 mm 클램프, choosePaper delegate 강참조, fill/fit 배율, nUpCellRects/nUpFeasible 60mm 게이트, NUpPageRenderer, PrinterStore 직행 인쇄, WYSIWYG 단일 출처 `renderWidth(for:)`) 기준으로 재작성
- `docs/design-docs/modules/services.md` — 파일 표에 `PrintService.swift`/`PrinterStore.swift` 누락 → 추가 (상세는 export-share.md 로 포인터)
- `docs/design-docs/modules/sheets.md`, `docs/design-docs/decision-log.md` 는 c266cb2 커밋에서 이미 갱신됨 — 추가 수정 불필요

## 테스트 감사 결과

- 모든 테스트가 최신 상태 — c266cb2 에 `PrintLayoutTests.swift` 신규(nUpCellRects 기하, nUpFeasible 게이트, 용지 mm→pt 변환, renderWidth fill/fit 모드, PrintOptions 구버전 JSON 하위호환 decode), `PrintServiceTests.swift`(옵션 모델 라운드트립/Equatable, printRenderWidth, fittedImageRect), `QA_Print_G2Tests.swift`(신규 20키 5로케일 존재·well-formed 계약) 갱신이 이미 포함
- 미커버 영역은 `PrinterStore` 의 UIPrinterPickerController/contactPrinter 와 `PrintService.present` 프레젠테이션 경로 — UIKit 실기기 의존이라 유닛 테스트 대상 아님

## 요약

최근 24시간 커밋 3건 중 실질 코드 변경은 인쇄 기능 확장 1건. 커밋에 decision-log 와 sheets.md 는 포함됐지만 인쇄 기능의 본 문서인 export-share.md 와 스펙 09-export-import.md, services.md 파일 표가 구버전으로 남아 있어 3개 문서를 현행 코드 기준으로 갱신했다. 테스트는 커밋에 기하 계약·하위호환·로케일 QA 가 모두 포함돼 추가 작업 없음. 소스 코드는 수정하지 않았다.