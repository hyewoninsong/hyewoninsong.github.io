---
project: "super-timetable"
date: "2026-04-08"
summary: "잠금 탭 팝업 → 시간 pill 오버레이, 테마 피커 색상 바 변경 등 11개 커밋 반영하여 UI 스펙 업데이트"
tags: ["docs"]
commits: 11
docsUpdated: 1
specsUpdated: 0
testsUpdated: 0
---

## 오늘의 변경 사항

- `f4341c0` improve: lock mode tap shows time pills instead of popup
- `e4a78ff` improve: edit mode UX + theme sheet cleanup
- `1071f14` improve: lock screen popup center align + day-of-week header color
- `6fdf43a` improve: resize snap 30min, preview accent color, share sheet dark mode
- `c4e61d4` improve: sync build number to 39
- `a6b6725` feat: show preset-required alert before opening preset sheet
- `bd37451` feat: time label offset above touch + white capsule style
- `dfcc801` improve: UX gap fixes — share sheet (G12, G13)
- `146f2af` improve: UX gap fixes — preset management (G8, G16)
- `ed0de8e` improve: UX gap fixes — grid interaction (G2,G3,G5,G9,G10,G14)
- `432ae1b` improve: sync build number to 37

## 문서 감사 결과

`docs/ui-spec-supertimetable.md` — 다음 항목 업데이트:

1. **Screen Map**: `Schedule Info Popup` → `Locked Tap Time Overlay`로 노드 변경
2. **Screen 1 (Main Grid — Locked)**: 블록 탭 동작 설명을 `ScheduleInfoOverlay` 팝업에서 `LockedTapTimeOverlay` (시간 pill 오버레이)로 변경. Edge case에 새 동작 추가.
3. **Screen 3**: 제목과 내용 전체를 `Schedule Info Popup`에서 `Locked Tap Time Overlay`로 재작성. 시작/종료 시간 pill (흰색 캡슐, 블록 위/아래)과 선택 테두리 동작 기술.
4. **Screen 2 (Main Grid — Editing)**: 드래그 생성 최소 스냅 5분 → 10분으로 업데이트.
5. **Screen 4 (Block Selected State)**: 리사이즈 스냅 `editSnapMinutes = 10` → `GridConstants.resizeSnapInterval = 30`으로 업데이트.
6. **Screen 6 (Theme Picker Sheet)**: G9 미니어처 그리드 프리뷰 설명 삭제. 30-세그먼트 색상 바 + 흰색 테마 이름 오버레이 방식으로 교체. 레이아웃 스케치 업데이트.

## 테스트 감사 결과

프로젝트에 별도 Swift 테스트 타겟이 없음 — 감사 대상 없음

## 요약

오늘은 11개 커밋이 병합되었으며, 대부분 UX 갭 수정(G2~G16 시리즈)과 UI 개선에 집중되어 있었습니다. 가장 큰 스펙 변경은 잠금 모드 블록 탭 시 전체 정보 팝업(`ScheduleInfoOverlay`) 대신 시작/종료 시간 pill만 표시하는 `LockedTapTimeOverlay`로의 전환입니다. 테마 피커도 미니어처 그리드 프리뷰 대신 30-세그먼트 색상 바로 변경되었습니다. `docs/ui-spec-supertimetable.md`를 현재 코드 상태에 맞게 전면 업데이트했습니다.