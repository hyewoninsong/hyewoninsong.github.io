---
project: "super-timetable"
date: "2026-09-12"
summary: "24시간 내 48개 커밋 확인 — 문서·스펙·테스트 모두 이미 최신 상태, 추가 수정 없음"
tags: []
commits: 48
docsUpdated: 0
specsUpdated: 0
testsUpdated: 0
---

## 오늘의 변경 사항

최근 24시간 내 48개 커밋 (주요 항목):
- feat(sheets): 설정·인쇄·공유 선택 UI 통일 + 일정 글자 색(밝은색/어두운색/자동) 신설 (#388)
- fix(sheets): 메뉴 스크롤 시 라벨 분리(#390), 칩 라벨 폭 semibold 고정(#389)
- feat: iPad 멀티윈도우(#367), iPad 포인터 hover/보조클릭(#368), 창 제목·리사이즈 핸들 포인터(#370)
- fix: iPhone 터치 회귀 — indirectPointer 가드(#371) + 사후 기록(#375)
- feat: 알람 잠금 화면 카드 Live Activity v2(#376)
- feat(print): 글자 크기 3종 지정(#377), 글자 크기 하한(#366)
- fix: 저장 스키마 변경 내성(#378), 입력 길이 상한·파일명 sanitize·일정 개수 상한(#379)
- fix: 시간표 로드/저장 실패 제보(#380), 죽은 iCloud 동기화 제거(#381)
- chore: PrivacyInfo.xcprivacy 추가(#383), 미사용 설정 정리(#382)
- 그 외 스타일 팝오버·인쇄 시트 지면 관련 다수 개선

## 문서 감사 결과

모든 문서가 최신 상태 — 48개 커밋을 전수 확인한 결과, 각 PR이 이미 자체적으로 관련 모듈 문서(`settings.md`/`10-settings.md`, `export-share.md`/`09-export-import.md`, `models.md`/`store.md`, `widgets.md`, `ipad.md`, `gestures.md`, `sheets.md`)를 함께 갱신해 두었음. 결정 기록(`docs/design-docs/decisions/`)도 48개 커밋 전부 대응 파일이 존재함을 확인.

## 테스트 감사 결과

모든 테스트가 최신 상태 — 신규 로직(일정 글자 색, 인쇄 글자 크기, 저장 스키마 내성, 파일명 sanitize, 입력·일정 개수 상한, 알람 formatVersion 2, iPad 포인터/멀티윈도우/창 제목, 저항 물리)에 대응하는 테스트 파일(`TextLimitsTests`, `ScheduleTextColorModeTests`, `SaveDataHardeningTests`, `PrintServiceTests`, `ScheduleAlarmServiceTests`, `ResistanceFeelTests`, `GridPointerTests`, `MultiWindowSceneStateTests` 등)이 이미 각 PR에 포함되어 있음을 확인.

## 요약

이번 배치는 결정 로그·postmortem 규칙을 잘 지킨 사례로, 48개 커밋 모두 문서와 테스트가 PR 안에서 함께 처리되어 별도 보강이 필요 없었다. 특이사항으로 `2026-09-11-alarm-title-format.md` 결정이 `alarm-live-activity.md`(#376)에서 뒤집혔는데, 이는 기존 파일을 건드리지 않고 새 파일로 대체하는 프로젝트 관행을 정확히 따른 것이라 문제는 아니다.