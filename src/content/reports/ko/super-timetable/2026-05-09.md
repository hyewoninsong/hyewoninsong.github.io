---
project: "super-timetable"
date: "2026-05-09"
summary: "color-picker v10(colorSource) 마이그와 신규 AppSettingsSheet 반영해 모듈 문서 4건 + 스펙 1건 동기화"
tags: ["docs", "specs"]
commits: 13
docsUpdated: 4
specsUpdated: 1
testsUpdated: 0
---

## 오늘의 변경 사항

- `24c497f` docs: pipeline summary, decision log entry for app settings sheet
- `c0efb1a` chore(pipeline): checkpoint qa_run PASS for dev1 (G1) and dev2 (G2)
- `6ee4bdf` chore(pipeline): checkpoint qa_run artifacts before --rerun-from qa_run
- `7133f41` fix(tests): collapse string-literal concatenations to single literals
- `a7a762d` chore(pipeline): checkpoint after dev/test stages — pre qa_review rerun
- `c81fb86` docs(pipeline): clarify spec for app settings sheet
- `1fd8230` docs(decision-log): add color-picker follow-up decisions
- `044d873` fix(color-picker): tab switch race — bind Picker directly + onChange side-effects
- `433c393` fix(color-picker): plus button last; tab switch single-tap; sheet single detent
- `50040fd` fix(color-picker): hide grid while inline editor is expanded
- `2248342` feat(color-picker): preset/custom tabs, paging, inline editor
- `040e0f9` docs(pipeline): clarify stage — color picker revamp
- `48a0b7c` chore: sync prior-session WIP changes (carousel sheet removal, widget cleanup)

핵심 변경은 두 갈래: (1) 색상 모델 v9 `colorIndex: Int` → v10 `colorSource: ColorSource(.preset/.custom)` 마이그 + ColorPickerView 개편(`InlineColorEditor.swift` 신설), (2) 신규 `AppearanceSettings.swift` + `AppSettingsSheet.swift` (외관 picker + 현재 시각/요일 토글).

## 문서 감사 결과

업데이트한 파일:

- `docs/design-docs/modules/presets-themes.md` — v9 colorIndex 기반 설명 전체를 v10 colorSource 로 재작성. mermaid 다이어그램, 파일 표(`InlineColorEditor.swift`, `CustomColorStore.swift` 추가), v9→v10 마이그 로직, 위젯 동기화, 사용자 색 편집/삭제 시 일정 처리 보강.
- `docs/design-docs/modules/models.md` — `TimetableModel.swift` (v7 → wrapper v9) / `Schedule.swift` (`colorSource: ColorSource` v10) / `TimetableTheme.swift` (30색 → 20색) 표 갱신, presets-themes 문서로 cross-link 추가.
- `docs/design-docs/modules/settings.md` — `AppearanceSettings.swift` + `AppSettingsSheet.swift` 항목 추가, 설정 항목 표에 외관 모드 추가, AppSettingsSheet 동작 설명 추가.
- `docs/design-docs/modules/store.md` — `AppearanceSettings.swift` 행 추가, `CustomColorStore.swift` 설명을 위젯 미러링 포함해 정확하게 갱신.
- `docs/product-specs/10-settings.md` — 전역 설정 표에 외관 모드 행 추가, AppSettingsSheet ASCII 와이어프레임 신설, DisplaySettingsSheet 와이어프레임을 실제 아코디언 동작에 맞게 수정, 접근 방법 분리 (... 메뉴 vs 슬라이더 아이콘).

`docs/design-docs/modules/sheets.md` 와 `docs/product-specs/05-presets-themes.md` 는 이번 커밋에 포함되어 이미 최신.

## 테스트 감사 결과

모든 테스트가 최신 상태. QA 파이프라인이 변경 영역을 이미 광범위하게 커버:

- **App Settings**: `AppSettingsFoundationTests.swift` (G1 — AppearanceMode 값 의미, persistence, ShareSheet 기본 해석, 4개 로케일 키), `AppSettingsRegressionTests.swift` (GR), `SettingsMenuIntegrationTests.swift` (G2)
- **Color picker / ColorSource**: `ColorPickerRegressionTests.swift`, `ColorRenderingDispatchTests.swift`, `ColorSourceModelTests.swift`, `InlineColorEditorTests.swift`, `QuickEditColorSourceTests.swift`, `InfoQuickEditSheetLogicTests.swift`
- **마이그레이션**: `CodableMigrationTests.swift`, `MigrationTests.swift`, `TimetableModelTests.swift`, `TimetableStoreTests.swift` 모두 v10 으로 갱신됨

## 요약

지난 24시간의 두 핵심 변경(color-picker v10 + AppSettingsSheet)에 맞춰 노후화된 모듈 문서 4건과 spec 1건을 동기화했어. presets-themes/models/settings/store 모듈 문서가 v9 colorIndex 기준으로 멈춰 있었던 게 가장 큰 격차였고, 그 외 10-settings spec 의 와이어프레임이 새/구 시트를 구분하지 못하던 부분도 정리했어. 테스트 쪽은 QA 파이프라인이 이미 G1/G2/GR + color-picker 6종을 만들어 둔 상태라 추가 작성 없이 통과.