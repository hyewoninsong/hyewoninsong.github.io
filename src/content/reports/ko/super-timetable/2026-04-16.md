---
project: "super-timetable"
date: "2026-04-16"
summary: "two-detent 시트, batch 캡처, 테마 버튼 개선 커밋 2건에 대한 문서·테스트 감사 및 업데이트"
tags: ["docs", "specs", "tests"]
commits: 2
docsUpdated: 2
specsUpdated: 1
testsUpdated: 1
---

## 오늘의 변경 사항

- `1125fbc` — 테마 툴바 버튼에 파란 Capsule 배경 추가 및 `paintbrush.fill` 아이콘 제거 (작업 트리의 미스테이지 변경은 이를 다시 배경 없음 + primary 텍스트로 되돌려 CLAUDE.md 결정 로그와 일치)
- `b753854` — `InfoQuickEditSheet` two-detent 전환(기본 collapsed, 시각 피커 열릴 때 expand), `capturedSimilarSchedules` 시트 열릴 때 스냅샷 캡처(`.task` 사용), 배치 업데이트 행 항시 표시 + `batchEditEnabled` 토글 (요일·시각 섹션 blur + 그리드 하이라이트)

## 문서 감사 결과

- **`docs/design-docs/modules/sheets.md`** 업데이트 — two-detent 높이/로직, `focusedTimeField` 토글 동작, `capturedSimilarSchedules` 스냅샷 메커니즘, `batchEditEnabled` 토글 + `onBatchHighlightChange` 콜백 반영
- **`docs/design-docs/modules/presets-themes.md`** 업데이트 — `comboChanged`/`similarSchedules.isEmpty` 조건을 `capturedSimilarSchedules` 스냅샷 패턴으로 교체, colorIndex 0 유효 명시

## 테스트 감사 결과

- **`docs/product-specs/04-schedule-editing.md`** 업데이트 — 기존 배치 조건(`comboChanged`, `similarSchedules`)을 현재 스냅샷 기반 토글 동작으로 수정
- **`TimetableTests/InfoQuickEditSheetLogicTests.swift`** 전면 재작성 — 더 이상 존재하지 않는 `SchedulePreset` / `Schedule(presetID:)` 참조 제거 후 다음 테스트 추가:
  - `ScheduleSuggestionsTests` — 제목+colorIndex 조합 빈도 기반 필터/정렬
  - `CapturedSimilarSchedulesTests` — 스냅샷 로직, colorIndex 0 유효성, 부분 불일치 제외 확인
  - `BatchSaveAllTests` — 제목+colorIndex 일괄 전파 검증
  - `HandleSaveTriggerTests` — `comboChanged` 제거 후 `capturedSimilarCount` 기반 조건 검증
  - `TwoDetentLogicTests` — collapsed/expanded 높이 계산, `focusedTimeField` → detent 선택 로직

## 요약

최근 24시간 내 커밋 2건(two-detent 시트, 배치 캡처 개선, 테마 버튼 변경)에 대해 문서 2건, 명세 1건, 테스트 1건을 갱신했습니다. 기존 테스트 파일이 삭제된 모델(`SchedulePreset`, `presetID`)을 참조하고 있어 전면 재작성이 필요했으며, 문서는 `capturedSimilarSchedules` 스냅샷 패턴 및 two-detent 동작을 반영해 업데이트했습니다.