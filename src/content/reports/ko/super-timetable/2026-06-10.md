---
project: "super-timetable"
date: "2026-06-10"
summary: "통합 색상 팔레트(hex 단일화 v11) 기능에 맞춰 문서 4건·스펙 3건을 최신화; 테스트는 이미 완비"
tags: ["docs", "specs"]
commits: 4
docsUpdated: 4
specsUpdated: 3
testsUpdated: 0
---

## 오늘의 변경 사항

- `33e665b` test: widget v9 매핑 단언 모순 수정 (QA_G4_8 vs QA_GR_4)
- `0b1b990` test: MigrationTests v6 체인 단언을 hex-only 모델에 맞게 수정 (.preset → .custom)
- `a82c90a` feat: 통합 색상 팔레트 — hex-only 색 모델 + 피커 재작업 (핵심 변경)
- `bb80c9a` docs(pipeline): 통합 색상 팔레트 clarify (프리셋 인덱스 제거 + UI)

## 문서 감사 결과

핵심 변경(`ColorSource.preset(Int)` 제거, `ScheduleVersion.v11`, encode 의 legacy `colorIndex` 제거, 단일 페이지드 팔레트, 프리셋/사용자화 탭 폐지)에 맞춰 stale 문서를 갱신:

- `docs/design-docs/modules/presets-themes.md` — Mermaid/표를 hex-only 로 정정, `resolvedColor()`·similarityKey 단일 분기, v10→v11 마이그 섹션 추가, encode 의 colorIndex 동시기록 제거 반영
- `docs/design-docs/modules/widgets.md` — `resolvedColor()` 의 `.preset(i)` 분기 제거, 위젯의 legacy decode 폴백 유지 명시
- `docs/design-docs/modules/sheets.md` — 색상 팝오버 섹션을 탭 구조 → 단일 페이지드 그리드(+ trailing `+`)로 재작성, 중복 추가 허용 안내, 2026-06-10 변경 로그 추가
- `docs/design-docs/decision-log.md` — 2026-06-10 hex 단일화/통합 팔레트 결정 항목 추가 (향후 유사 케이스 가이드 포함)
- `docs/product-specs/01-data-model.md` — `ColorSource` 를 hex-only `.custom(String)` 로 정정
- `docs/product-specs/05-presets-themes.md` — "v11+" 헤더, enum 스니펫, 프리셋 vs 사용자화 개념·2탭 표·페이지 상태표·similarityKey·v9→v10 encode 섹션 정정 (마이그 히스토리는 보존하되 결과만 hex 로 수정)
- `docs/product-specs/04-schedule-editing.md` — 배치 편집 유사 일정 기준을 `colorIndex` → `colorSource`(같은 hex = 같은 그룹)로 정정

참고: `TimetableVersion.current` 는 여전히 v10 (ScheduleVersion 만 v11) 이므로 `models.md` 의 "v10" 표기는 정확하여 그대로 둠.

## 테스트 감사 결과

- 모든 테스트가 최신 상태 — 추가/수정 없음
- 기능 커밋이 이미 `QA_UCP_G1/G2/GR` 신규 3파일(@Test 43개: 모델 마이그레이션·위젯 미러·통합 회귀·시드·similarityKey·에디터)과 기존 ~15개 테스트 파일 갱신을 포함했고, 오늘의 최신 2개 커밋이 테스트 단언 정합성 fix 였음. 미커버 영역이 없어 중복 테스트를 추가하지 않음

## 요약

오늘은 "통합 색상 팔레트(hex-only v11)" 단일 기능이 핵심 변경이었음. 코드는 `ColorSource` 를 hex 단일 표현으로 단순화하고 프리셋/사용자화 탭을 단일 페이지드 팔레트로 통합했는데, 이에 따라 디자인 문서 4건과 제품 스펙 3건이 구 모델(`.preset(Int)`, 2탭 UI, encode 의 colorIndex 동시기록)을 설명하고 있어 모두 v11 hex-only 기준으로 정정했다. 테스트는 파이프라인 QA 트랙이 이미 포괄적으로 작성·정합화해 둔 상태라 추가 작업이 불필요했다. 소스 코드는 수정하지 않았다.