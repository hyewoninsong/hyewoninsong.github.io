---
project: "super-timetable"
date: "2026-06-03"
summary: "표시설정 컴팩트화 관련 문서 2건 갱신, narrow-slot 생성 경로 회귀 테스트 1건 추가"
tags: ["docs", "specs", "tests"]
commits: 7
docsUpdated: 1
specsUpdated: 1
testsUpdated: 1
---

## 오늘의 변경 사항

- `39a807f` docs: pipeline artifacts + decision-log for compact rows
- `d83818f` improve: compact display-settings day grid & start-day chips
- `2efbf0e` docs: clarify display-settings compact rows pipeline
- `c6e4175` chore: remove debug prints & unify narrow-slot create path (#94)
- `dc90569` fix: drag-create preview persistence, new-timetable sheet polish, end-label occlusion return (#93)
- `4487e11` improve: show day grid as current value in display-settings days header (#92)
- `d431c13` chore: bump build number to 141 (TestFlight deploy) (#91)

## 문서 감사 결과

`DisplaySettingsSheet` 의 "요일 설정" 섹션이 텍스트 요약(`dayRangeSummary`) 대신 `DaySelectionGrid` 네모 그리드를 헤더 현재값으로 항시 노출하도록 바뀌었고, `요일범위_*` 로케일 키와 계산 속성이 제거되었으나 일부 문서가 옛 설계를 기술하고 있어 갱신함:

- `docs/design-docs/modules/sheets.md` — detent 높이 표(240/490/352 → 257/435/369), 헤더 양식의 `Text(value)` → `@ViewBuilder value()`, `dayRangeSummary`/`요일범위_*` 제거 및 `DaySelectionGrid` 항시 노출 설명, `DayCell` 정사각형·시작요일 칩 44×32 컴팩트화 내용 반영
- `docs/product-specs/10-settings.md` — 표시 설정 시트 와이어프레임을 "표시 범위 체크박스" 에서 "요일 네모 그리드(현재값, 항시 노출) + 펼침 시 시작요일/토·일 토글" 구조로 수정

나머지 문서(`grid.md`, `gestures.md`, `02-grid-view.md`, `03-gestures.md`)는 이번 변경의 영향이 없어 최신 상태. `decision-log.md` 는 이미 `39a807f` 에서 갱신됨.

## 테스트 감사 결과

`d83818f`(컴팩트화)와 `dc90569`(드래그생성/엔드필/새시트)는 커밋에 테스트가 포함되어 충분히 커버됨. 다만 `c6e4175`(#94)의 narrow-slot 생성 경로 통합 — `onNarrowSlotPressed` 가 더 이상 `dragState`/`isCreatingSchedule`/`highlightedTimes` 를 재초기화하지 않고 드래그 경로와 동일하게 미리보기 박스를 유지 — 은 테스트 없이 머지됨. 기존 G2 소스감사 스위트는 `handleDragEndCreateSchedule` 헬퍼만 감사하고 narrow-slot 호출부는 보지 않아 회귀를 놓칠 수 있어 보강:

- `TimetableTests/NarrowSlotCreatePathRegressionTests.swift` (신규) — (1) narrow-slot 탭이 통합 헬퍼를 호출하는지, (2) 미리보기 상태를 자체적으로 재초기화하지 않는지, (3) `[startPill]` 디버그 print 가 제거된 상태를 유지하는지 소스 레벨로 감사. build-for-testing 성공 + 3개 테스트 모두 통과 확인.

## 요약

최근 24시간 내 7개 커밋을 감사함. 표시설정 컴팩트/그리드 헤더 변경에 따라 stale 해진 모듈 문서 1건과 스펙 1건을 코드 현재 상태에 맞게 갱신했고, 테스트 없이 머지된 #94 의 narrow-slot 생성 경로 통합을 잠그는 회귀 테스트 파일 1건을 추가해 빌드·실행으로 검증 완료. 소스 코드는 수정하지 않음.