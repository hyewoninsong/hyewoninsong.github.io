---
project: "super-timetable"
date: "2026-06-01"
summary: "색상시트·목록 UI 폴리시 변경에 맞춰 모듈 문서와 스펙 2건 갱신, 테스트는 이미 최신"
tags: ["docs", "specs"]
commits: 3
docsUpdated: 1
specsUpdated: 1
testsUpdated: 0
---

## 오늘의 변경 사항

- `923fa81` docs: pipeline artifacts + decision-log for color-sheet/list polish
- `414ffd4` fix: color-sheet & timetable-list UI polish (소스 3 + 테스트 2 파일)
- `400c607` docs(pipeline): clarify color-sheet & list UI polish

핵심 코드 변경 3가지:
1. `ColorPickerView` — 인라인 색상 에디터(`isPickerExpanded`) 열림 시 세그먼트 탭(`tabPicker`)·편집/완료 토글 숨김
2. `TimetableGridView` — 이름변경 alert 문구를 기존 `"Enter Timetable Name"` 키로 재사용해 다국어화
3. `TimetableListSheet` — 편집 모드에서도 현재 카드 파란 선택 테두리 유지 (`isSelected` 에서 `!isEditMode` 제거)

## 문서 감사 결과

- `docs/design-docs/modules/sheets.md` (docs) — 색상피커 레이아웃 섹션에 "에디터 확장 시 탭·편집 토글 숨김" 동작 누락 → 다이어그램 아래 설명 추가
- `docs/product-specs/06-timetable-list.md` (specs) — 편집 모드 섹션에 "현재 카드 파란 선택 테두리 유지" 동작 누락 → 항목 추가
- `decision-log.md` 는 커밋에서 이미 3건 결정 모두 기록됨 (추가 작업 불필요)

## 테스트 감사 결과

- 모든 테스트가 최신 상태 — `414ffd4` 가 이미 `ColorSheetListPolishG1Tests.swift`(10 테스트)와 `ColorSheetListPolishRegressionTests.swift`(태스크 1~3 + 로컬라이즈 무결성)를 추가해 3가지 변경을 모두 커버. 미커버 기능 없음, 추가 불필요.

## 요약

최근 24시간 내 색상시트/목록 UI 폴리시 파이프라인 작업(3 커밋)이 있었음. 소스 변경과 테스트, decision-log 는 파이프라인이 이미 완비한 상태였으나, 모듈 문서(`sheets.md`)와 제품 스펙(`06-timetable-list.md`)이 신규 동작을 반영하지 못해 각각 1건씩 보강함. 테스트 커버리지는 완전해 추가 작업 없음.