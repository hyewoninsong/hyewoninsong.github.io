---
project: "super-timetable"
date: "2026-04-22"
summary: "Color.brand 제거 리팩터로 인한 잠금 오버레이 동작 변경을 ui-spec에 반영"
tags: ["specs"]
commits: 2
docsUpdated: 0
specsUpdated: 1
testsUpdated: 0
---

## 오늘의 변경 사항

- `7dae357` refactor: drop Color.brand, switch to Color.primary app-wide + remove center lock overlay
- `37c8646` Merge remote-tracking branch 'origin/main' into feat/sheet-improvements-v3

## 문서 감사 결과

- `docs/product-specs/ui-spec-supertimetable.md`
  - L61 Grid body 행: "long-press → shows lock overlay flash (1 s auto-dismiss)" → "long-press → bottom-left lock button ripple pulse (`LockRipple`)" 로 수정
  - L103 Edge cases: 중앙 `lock.fill` 1초 플래시 오버레이 기술을 `LockRipple` (3중 동심원, 0.9초 ease-out, `lockRippleTrigger &+= 1`) 로 갱신하고 2026-04-22 제거 사실 명시
- `decision-log.md` 는 커밋에서 이미 2026-04-22 항목(brand 제거 + 중앙 오버레이 제거) 이 추가되어 최신 상태
- 모듈 CLAUDE.md 들은 커밋에서 이미 stale 섹션이 정리됨
- `03-gestures.md` 등 타 스펙은 제거된 중앙 오버레이를 별도로 기술하지 않아 수정 불필요

## 테스트 감사 결과

- 모든 테스트가 최신 상태
- 테스트 코드에서 `showLockOverlay` / `Color.brand` / `LockRipple` 참조 없음 (grep 결과 없음)
- 이번 리팩터는 순수 시각/색상 치환 및 오버레이 제거로 새 로직이 없어 UI 테스트 추가 불요

## 요약

오늘의 변경은 `Color.brand` 전면 폐기 + 잠금 상태 중앙 오버레이 제거라는 시각 리팩터였다. decision-log 와 모듈 CLAUDE.md 는 커밋에 이미 포함되어 있었으나 `ui-spec-supertimetable.md` 의 Grid body / Edge cases 두 군데에 구 동작(1초 `lock.fill` flash)이 그대로 남아 있어 현재 구현(`LockRipple` 좌하단 물결)과 일치하도록 수정했다. 테스트는 제거된 심볼을 참조하지 않으며 새 로직이 없어 추가 대상 없음.