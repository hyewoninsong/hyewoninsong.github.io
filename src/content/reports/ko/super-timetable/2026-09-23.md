---
project: "super-timetable"
date: "2026-09-23"
summary: "커밋 13개 감사: 시트 모듈 문서의 옛 팔레트 설명을 고치고, GA4 등록 스펙 대조 계약 테스트를 추가함"
tags: ["docs", "tests"]
commits: 13
docsUpdated: 1
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- 673ead2d docs: 결정 로그 인덱스 자동 갱신 [skip ci]
- 56620e24 feat(palette): 기본 10색을 iOS 시스템 팔레트로, 노랑만 차콜 제목, 파스텔은 한 단계 진하게 (#510)
- c1807f7d docs: 결정 로그 인덱스 자동 갱신 [skip ci]
- 217d418e feat(analytics): 튜토리얼 퍼널·인쇄·알람 권한·실패 빈도·사용자 속성 이벤트 추가 + GA4 측정기준 13개 등록 (#509)
- 4dccd8e7 docs: 결정 로그 인덱스 자동 갱신 [skip ci]
- fd25f552 feat(appstore): 키워드 예산을 학년·사용자층 토큰으로 채우고 필드 겹침을 스크립트가 잡음 (#508)
- 7726442c chore(release): 마케팅 버전 1.0 → 1.0.1 (#507)
- ebe9c9a4 docs: 결정 로그 인덱스 자동 갱신 [skip ci]
- b0cd291a fix(analytics): 매개변수를 GA4 측정기준으로 등록할 수 있는 형태로 바꾸고 죽은 이벤트 정리 (#504)
- 2d786178 docs: 결정 로그 인덱스 자동 갱신 [skip ci]
- 36698af5 fix(icon): 앱 아이콘 재제작, 그림자 제거·벡터 렌더·iOS 시스템색 (#506)
- 71899b38 docs: 결정 로그 인덱스 자동 갱신 [skip ci]
- 4ed974ac feat(appstore): 핵심 검색어를 이름·부제·키워드 세 필드에 모두 넣음 (#505)

## 문서 감사 결과

- `docs/design-docs/modules/sheets.md`: 기본 탭 팔레트 설명이 아직 2026-09-10 OKLCH 세트 기준이었다. #510 기준(진한 10색은 iOS 시스템색이고 노랑만 차콜 제목, 파스텔은 L 0.85·C 0.12로 재선정)으로 고쳤다.
- presets-themes 모듈 문서, 05 스펙, 아이콘 파이프라인 문서, Store/CLAUDE.md, firebase-analytics-plan은 해당 PR에서 이미 갱신돼 있었다.
- `docs/adhoc/basic-palette-refresh/`에 남은 옛 hex 값은 당시 작업 기록이라 손대지 않았다.
- 버전 1.0.1 올림(#507)은 고칠 문서가 없었다.

## 테스트 감사 결과

- `TimetableTests/AnalyticsParameterContractTests.swift`에 `everyParameterIsRegisteredInGA4Spec`를 추가했다.
  - 보내는 이벤트 매개변수와 사용자 속성이 전부 `scripts/ga4-params.json`에 등록돼 있는지 본다. 문자열 값은 dimensions, 숫자 값은 metrics 쪽이어야 한다.
  - 반대 방향도 본다. legacy가 아닌 등록 항목 중 실제로 보내는 이벤트가 없는 게 있으면 실패한다.
  - #509는 "새 매개변수는 같은 PR에서 등록"을 규칙으로만 적어 뒀는데, 이제 테스트가 이를 강제한다.
  - `xcodebuild test -only-testing:TimetableTests/AnalyticsParameterContractTests`: 17개 모두 통과했고, 새 테스트도 그중 하나로 돌았다.
- #504·#506·#509·#510은 해당 PR에서 테스트가 이미 추가·갱신돼 있었다.
- #508의 `scripts/check_appstore_metadata.py`는 Python 테스트 환경이 없어 테스트를 추가하지 않았다. 직접 돌려 보니 통과(exit 0)였다.

## 요약

최근 24시간 동안 커밋 13개(#504~#510)가 들어왔다. 대부분의 PR이 문서와 테스트를 스스로 챙겨서 빠진 건 두 가지였다. 하나는 시트 모듈 문서에 남아 있던 옛 팔레트 설명이고, 다른 하나는 GA4 등록 스펙과 앱 코드가 어긋나는 걸 잡아 줄 장치였다. 둘 다 고쳤고, 소스 코드는 건드리지 않았다.