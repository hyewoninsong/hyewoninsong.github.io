---
project: "super-timetable"
date: "2026-06-04"
summary: "#96 요일칩 고정 높이 회귀 수정에 맞춰 sheets.md 모듈 문서 1건 갱신"
tags: ["docs"]
commits: 2
docsUpdated: 1
specsUpdated: 0
testsUpdated: 0
---

## 오늘의 변경 사항

- `74609b7` fix: day chips use fixed height instead of aspectRatio (circle regression) (#96) — `DisplaySettingsSheet.swift` 의 `DayCell` 을 `aspectRatio(1, .fit)` 에서 `.frame(height: 34)` 고정 높이로 변경 (동그라미化 회귀 수정), decision-log.md 동시 갱신
- `799b2f5` chore: bump build number to 144 (TestFlight deploy) — 빌드 번호 bump, 문서/테스트 영향 없음

## 문서 감사 결과

- `docs/design-docs/modules/sheets.md` 업데이트: 라인 186 이 #94 후속 컴팩트화에서 `DayCell` 을 `aspectRatio(1, .fit)` 정사각형으로 설명하고 있었으나, #96 에서 고정 높이(34)로 회귀 수정됨. 해당 내용을 반영하고 동그라미化 회귀 원인·해법 설명 추가
- `docs/product-specs/10-settings.md` 명세는 요일칩 구현 세부(프레임 방식)를 다루지 않아 영향 없음 — 최신 상태

## 테스트 감사 결과

- 모든 테스트가 최신 상태. #96 은 detent 상수(`collapsedHeight=257` 등)를 변경하지 않은 순수 시각(frame height) 수정이라 `DisplaySettingsDetentTests.swift` 의 미러링 로직은 그대로 유효
- 변경된 부분(고정 높이 리터럴 34)은 추출 가능한 로직이 아니라 SwiftUI View body 의 구현 세부이며, decision-log 에 명시된 대로 시뮬레이터 실기 캡처(`swiftui-visual-check`)로 검증됨 — 의미 있는 유닛 테스트 추가 대상 아님

## 요약

최근 24시간 내 커밋 2건(요일칩 회귀 수정 #96, 빌드 번호 bump) 중 #96 으로 인해 `sheets.md` 의 `DayCell` 설명이 실제 코드와 어긋나 1건 갱신했다. 명세와 테스트는 이번 변경의 영향을 받지 않아 최신 상태를 유지한다.