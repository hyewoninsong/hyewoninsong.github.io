---
project: "claude-monitor"
date: "2026-04-09"
summary: "8개 커밋 반영 — 프로젝트 집계 및 토큰 분석 스펙 2개 신규 작성, 백엔드 테스트 15개 추가"
tags: ["docs", "specs", "tests"]
commits: 8
docsUpdated: 0
specsUpdated: 2
testsUpdated: 2
---

## 오늘의 변경 사항

- `fix: add bun-types for test typecheck + .claudeignore`
- `feat: token usage analysis panel with reduction suggestions`
- `docs: update CLAUDE.md to reflect current architecture`
- `fix: add auth header to all API requests + regression tests`
- `chore: ignore tsbuildinfo and .claude dir`
- `feat: project-centric 3-level navigation UI`
- `feat: add GET /api/projects with 24h token aggregation`
- `remove: discord PR notification workflow`

## 문서 감사 결과

기존 문서(CLAUDE.md, docs/architecture.md)는 최신 상태로 확인됨. 신규 스펙 2개 작성:

- **`specs/token-usage-analysis.md`** (신규): `ProjectAnalysis.tsx`의 효율성 지표 공식, 제안 규칙 임계값(severity별 7개 룰)을 완전히 명세
- **`specs/project-aggregation.md`** (신규): `getProjects()` 알고리즘 전체 명세 — 세션 그루핑, 24h 토큰 집계, CLAUDE.md 라인 수 계산, `needsReview` 판단 로직, TTL 캐시(30s) 동작 방식 포함

## 테스트 감사 결과

백엔드 테스트 2개 파일 업데이트:

- **`packages/server/src/__tests__/project-service.test.ts`** (신규, 14개 테스트): `getProjects()` 전체 커버 — cwd 그루핑, 세션 카운트, 토큰 합산, lastActivityAt 선택, 정렬 순서, needsReview 플래그, projectName 파생, JSONL 누락 시 복원력
- **`packages/server/src/__tests__/session-log.test.ts`** (업데이트, +4개 테스트): `getSessionTokenStats24h()` 추가 — 파일 누락 시 0 반환, 24h 이내 항목만 포함, 24h 초과 항목 제외 검증

테스트 결과: 백엔드 45개 전체 통과 (기존 30 → 45), 프런트엔드 23개 전체 통과.

## 요약

최근 8개 커밋에서 신규 도입된 `GET /api/projects`, 3단계 네비게이션, 토큰 사용 분석 패널에 대한 문서·테스트 공백을 보완했다. 핵심 비즈니스 로직(`project-service.ts`, `session-log.ts` 24h 필터)에 대한 유닛 테스트가 없었으나 이번 감사에서 15개 테스트로 커버리지를 확보했다. 스펙 디렉터리(`specs/`)를 새로 신설하여 토큰 분석 공식과 집계 알고리즘을 문서화했다.