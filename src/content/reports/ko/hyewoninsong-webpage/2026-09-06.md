---
project: "hyewoninsong-webpage"
date: "2026-09-06"
summary: "스펙의 디렉토리 구조·배포 파이프라인·스크린샷 규칙을 실제 코드에 맞게 동기화하고, 평면 구조 앱 스크린샷 참조 검사를 테스트에 추가"
tags: ["specs", "tests"]
commits: 2
docsUpdated: 0
specsUpdated: 1
testsUpdated: 1
---

## 오늘의 변경 사항

- `a3a98a9` docs: daily audit reports 2026-09-05
- `9be3dba` docs: sync website spec with platforms schema and static app pages; add screenshot test

두 커밋 모두 문서·테스트 커밋이고 소스 코드 변경은 없었다. `9be3dba`는 어제 감사 결과(PR #22)이고, `a3a98a9`는 외부 일일 감사 잡이 `src/content/reports/ko/<project>/2026-09-05.md` 13개를 다시 추가한 커밋이다.

## 문서 감사 결과

- `docs/specs/website.md` 업데이트 (스펙 1건)
  - 2장 사이트 구조에 실제로 빌드되는 `/ko/privacy`, `/en/privacy` 페이지를 추가했다.
  - 4.2.2 스크린샷 규칙이 언어별 `public/apps/<slug>/<locale>/` 구조만 설명하고 있었는데, superfont·notequiz·supertimers는 언어 공통 평면 구조 `public/apps/<slug>/<name>.png`에 절대 경로로 참조한다는 점과 mathmaster는 스크린샷이 없다는 점을 명시했다.
  - 7장 디렉토리 구조가 초기 설계(`src/content/config.ts`, fontbox.md, MarkdownLayout/BlogPost 레이아웃, TagList/Pagination 컴포넌트, `public/images/`)에 머물러 있어 실제 저장소 기준으로 다시 썼다. `src/content.config.ts`, 앱 5개의 정적 상세 페이지, `BaseLayout.astro` 단일 레이아웃, 실제 컴포넌트 5개, `tests/`, `.github/workflows/deploy.yml`, `public/CNAME`을 반영하고 astro.config.mjs에 i18n 옵션이 없다는 것도 적었다.
  - 8장 배포 파이프라인이 `gh-pages` 브랜치 배포로 적혀 있었는데, 실제 워크플로우는 `actions/upload-pages-artifact` + `actions/deploy-pages` 방식이라 이에 맞게 고쳤다.
- `docs/plans/website.md`는 과거 개발계획서라 그대로 두었다.

## 테스트 감사 결과

- `tests/app-screenshots.test.mjs` 업데이트 (테스트 1건)
  - 기존 테스트는 `img()` 헬퍼를 쓰는 timetable 페이지만 검사해서 superfont·notequiz·supertimers의 절대 경로 스크린샷 참조는 검사 대상 밖이었다. 모든 정적 앱 페이지에서 `/apps/...` 이미지 절대 경로를 추출해 `public/` 아래 실제 존재 여부와 en/ko 참조 집합 일치를 검사하는 테스트를 추가했다.
  - 평면 구조 앱 3개가 자기 폴더의 스크린샷을 최소 1장 이상 참조하는지도 검사한다.
  - `index.astro`가 앱 slug로 잡히던 문제를 제외 처리했다.
- 결과: 17개 모두 통과. superfont 스크린샷 하나를 임시로 빼면 해당 테스트가 실패하는 것을 확인한 뒤 복원했다. `astro build`도 25페이지 정상 생성.

## 요약

최근 24시간의 커밋은 소스 변경 없이 문서·테스트만 건드렸지만, 스펙의 디렉토리 구조와 배포 설명이 초기 설계에 머물러 실제 코드와 여러 곳에서 어긋나 있어 동기화했고, 스크린샷 참조 검사를 평면 구조 앱까지 확장했다. 변경은 PR #22 브랜치 위에 스택한 PR #23으로 올렸으며 병합 여부는 사용자 판단에 맡긴다.

주의할 점 하나: `a3a98a9`가 추가한 `src/content/reports/` 아래 13개 파일은 PR #19에서 reports 컬렉션과 페이지가 제거된 뒤라 어디서도 참조되지 않는 고아 파일이다. 빌드에는 영향이 없지만 외부 일일 감사 잡이 계속 이 파일을 만들어내고 있으니, 해당 잡을 끄거나 병합 전에 디렉토리를 삭제하는 것을 권한다. 이번 감사에서는 지침대로 소스 코드와 콘텐츠 파일은 건드리지 않고 보고서만 출력했다.