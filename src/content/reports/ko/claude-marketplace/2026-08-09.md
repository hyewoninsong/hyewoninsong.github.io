---
project: "claude-marketplace"
date: "2026-08-09"
summary: "appstore-assets v2.4 커밋 감사 — 문서 1건 보강, 셸 스크립트 테스트 18건 신규 추가, install.sh 배포 누락 발견"
tags: ["docs", "tests"]
commits: 1
docsUpdated: 1
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- `21c6994` improve(appstore-assets): v2.4 — segment recapture, deterministic gates, approved-shot vault
  - 신규 스크립트 5개(`approve.sh`, `gate-check.sh`, `preflight.sh`, `segment-map.sh`, `shot-digest.sh`), pytest 테스트 최초 도입(`tests/`), SKILL.md·에이전트 프롬프트·파이프라인 문서 대폭 개정 (25개 파일, +2228/-244)

## 문서 감사 결과

- `settings/docs/appstore-pipeline.md` — 커밋에서 이미 v2.4 전체(게이트, 세그먼트 재캡처, `.approved/` 보관, retry economics)를 반영함. 최신 상태.
- `skills/appstore-assets/SKILL.md` — 신규 스크립트 5개 모두 문서화되어 있음. 최신 상태.
- `docs/modules/skills.md` — 스킬이 `tests/` 폴더(pytest)를 동봉할 수 있다는 새 관례가 빠져 있어 한 줄 보강함. **(업데이트 1건)**
- `docs/modules/distribution.md`, `skills/CLAUDE.md` — 상위 수준 설명이라 변경 불필요.
- **소스 코드 이슈 발견 (수정 금지 대상이라 플래그만)**: `install.sh`의 appstore-assets 스크립트 다운로드 루프(169행, 465행)에 v2.4 신규 스크립트 5개가 전부 빠져 있고, 기존 `frame.sh`·`frame_screenshots.py`도 원래부터 누락되어 있다. 이대로면 `--global`/`--update` 설치 시 세그먼트 재캡처·게이트 기능이 배포되지 않는다. `install_global`과 `update_global` 두 루프 모두 수정 필요.

## 테스트 감사 결과

- `tests/test_frame_screenshots.py` — 커밋에 포함된 38개 테스트 실행 확인, 전부 통과.
- `tests/test_shell_scripts.py` — **신규 추가 (업데이트 1건)**. 커버리지가 없던 셸 스크립트를 보완:
  - 번들 스크립트 15개 전체의 `bash -n` 문법 검사 + 실행 권한 검사
  - `segment-map.sh` 기능 테스트: shot↔테스트 메서드 인덱스 생성, `--resolve`의 리터럴/패턴(`String(format:)`) 매핑, zsh 단어 분리 경로, 매핑 실패 시 exit 3 폴백
  - `shot-digest.sh` 기능 테스트: save/changed 사이클, 한 장만 바뀌면 그 shot만 출력
- 전체 스위트 56개 테스트 통과 (0.98s).

## 요약

최근 24시간 내 커밋은 appstore-assets v2.4 하나였고, 커밋 자체가 파이프라인 문서와 SKILL.md를 함께 갱신해서 문서 정합성은 양호했다. 모듈 문서에 새 `tests/` 관례만 보강했다. 테스트는 Python 헬퍼(frame_screenshots.py)만 커버하고 신규 셸 스크립트 5개가 비어 있어서, 문법 스모크 + 핵심 2개 스크립트(segment-map, shot-digest)의 기능 테스트 18건을 추가했다. 가장 중요한 발견은 `install.sh`에 v2.4 스크립트 배포 항목이 누락된 것 — 소스 코드라 감사에서 수정하지 않았으므로 별도 커밋으로 두 다운로드 루프에 추가해야 한다.