---
project: "claude-marketplace"
date: "2026-08-23"
summary: "human-feedback 멀티타겟 라우팅 반영해 SPEC/SKILL/CLAUDE.md 문서 갱신, 테스트는 이미 충분"
tags: ["docs", "specs"]
commits: 7
docsUpdated: 2
specsUpdated: 1
testsUpdated: 0
---

## 오늘의 변경 사항

- `1384f57` docs: pipeline artifacts for pipeline-feedback-kb
- `ccf1f6d` feat: pipeline human-feedback loop upgrades
- `e20a570` improve: doc-scout reads KB index.md first
- `22813cd` feat: add docs knowledge base structure
- `988a358` Merge pull request #61 (pipeline-doc-versioning)
- `fd01864` Merge pull request #62 (testflight-stale-branch-guard)
- `a06ed3e` feat(testflight): add stale branch guard to preflight

## 문서 감사 결과

`ccf1f6d`(human-feedback 루프 업그레이드)로 실제 동작이 크게 바뀌었는데, 문서 3곳이 옛 "단일 타겟" 방식을 그대로 서술하고 있어 업데이트함:

- `skills/pipeline/SPEC.md` — human review 단계 설명(stage [10]), 4-line VERDICT 헤더 설명, `index.json` 스키마, human feedback contract, 파일 네이밍 테이블, "Key rules" 항목을 전부 갱신. 핵심 변경점: `TARGET: task<N>` 단일 라인 → `## Target: task<N>` 섹션(라운드당 여러 개) + `TARGETS:` 종합 라인; `human_review.history` 항목에 `top` 필드와 `changelog_upto`/`promotion_proposed_targets` 필드 추가; `human-review/CHANGELOG.md`, `human-review/promotion-candidates.md` 신규 산출물 반영; doc-scout가 KB `index.md`를 먼저 읽는 규칙(`e20a570`) 추가.
- `skills/pipeline/SKILL.md` — frontmatter description과 "Human review" 섹션을 멀티타겟 라우팅 + 병렬 fix dispatch + CHANGELOG/promotion-candidates 흐름으로 갱신. doc-scout 문단에 KB index-first 동작 추가.
- `skills/CLAUDE.md` — pipeline 스킬 한 줄 설명을 "owning task node" → "every task node it touches"로 수정.
- `settings/docs/doc-policy.md`(`22813cd`에서 추가된 KB 카테고리 테이블 행)는 내용이 일치해 변경 없음 — 단, 같은 파일의 기존 `docs/pipeline/<branch>/` 라이프사이클 설명이 현재 그래프 노드 폴더 구조와 다른 건(오늘 커밋과 무관한 기존 drift) 별도 확인 필요.
- `docs/modules/distribution.md`, 루트 `CLAUDE.md`는 install.sh 변경과 충돌 없어 수정 불필요.

## 테스트 감사 결과

`ccf1f6d`/`e20a570`/`22813cd` 커밋이 이미 각각 `test_hf_multitarget.py`, `test_hf_kb_regression.py`, `test_doc_scout_persona.py`, `test_kb_structure.py`를 추가해 신규 함수(`_append_changelog_line`, `_guess_promotion_category`, `_append_promotion_candidates`, `_finalize_human_feedback_rounds`, `_parse_triage_sections`, `_parse_targets_line`, `_prepare_human_feedback_multi`, `_dispatch_dev_fixes_parallel` 등) 전부 커버 중 — 추가/수정한 테스트 없음.

## 요약

오늘 커밋 중 `ccf1f6d`가 human-feedback 로직을 단일 타겟에서 멀티 타겟 라우팅으로 크게 바꿨는데, 관련 스펙/스킬 문서 3개가 옛 동작을 서술하고 있어 SPEC.md/SKILL.md/skills/CLAUDE.md를 코드와 일치하도록 갱신했음. 테스트는 해당 커밋들이 이미 충분히 추가해둬서 추가 작업 없음.