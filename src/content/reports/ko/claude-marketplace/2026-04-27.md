---
project: "claude-marketplace"
date: "2026-04-27"
summary: "pipeline regression 스테이지/pause-after, match-image intent 모드 추가 — 문서·테스트는 커밋에 포함되어 정합. skills/CLAUDE.md 한 줄만 보강"
tags: ["docs"]
commits: 5
docsUpdated: 1
specsUpdated: 0
testsUpdated: 0
---

## 오늘의 변경 사항

- `fd4d786` feat(pipeline): integrated regression test stages (regression_review/regression_run, GR 그룹, owner attribution + dev-fix)
- `43f78f3` Merge PR #44 (pipeline pause-after)
- `378f3ac` feat(pipeline): `--pause-after STAGE` 게이트 (exit 8 + resume)
- `81acc5e` Merge PR #43 (match-image intent)
- `1cbb2bb` feat(match-image): intent 모드 + reviewer before/after 평가축

## 문서 감사 결과

- `skills/CLAUDE.md` — match-image 행을 "reproduce (target) or correct (intent)"로 업데이트, 폴더 표기를 `<branch>` → `<slug>`로 정정 (orchestrator는 `match-image/<slug>` 사용).
- `skills/pipeline/SPEC.md` — 커밋에서 이미 regression_review/regression_run 스테이지, `06c-…`/`11-regression-…` 파일 명명, pause-after 계약 모두 반영됨.
- `skills/pipeline/SKILL.md` — pause-after exit 8 처리 및 `--pause-after spec_fork` 사용법 반영됨.
- `skills/pipeline/lib/state.py` STAGE_NAMES와 `regression_runner` 세션 키가 SPEC와 정합.
- `skills/match-image/SKILL.md` — target/intent 모드 표·트리거·실행법·산출물 모두 반영됨.
- `docs/modules/skills.md`는 스킬별 세부를 다루지 않아 추가 변경 불필요.

## 테스트 감사 결과

- `skills/pipeline/tests/test_integration.py` — 신규 `test_pause_after_spec_fork`, `test_regression_run_fail_then_fix`, `test_pause_after_unknown_stage_errors` 추가됐고 happy-path도 `06c-…`/`11-regression-…`/`GR` 그룹/`regression_runner` 세션을 모두 검증. pause-after 테스트의 "later stages"에 `regression_review`/`regression_run`가 포함되어 있어 정합.
- `skills/pipeline/tests/fake_claude.py` — `handle_regression_reviewer`, `handle_regression_runner`(실패 주입 포함), 라우팅 우선순위까지 보강됨.
- `skills/match-image/` 는 기존부터 테스트 인프라가 없으며 (fake-claude shim 부재), intent 모드 단독으로 새 테스트 프레임워크를 도입하는 건 일일 감사 범위를 초과 — 별도 작업으로 분리 권장.

## 요약

오늘 머지된 3개 기능(pipeline regression stages, pipeline pause-after, match-image intent)은 각 커밋 안에서 SPEC/SKILL/테스트가 함께 갱신되어 코드와 문서가 정합 상태. 다만 `skills/CLAUDE.md`의 match-image 한 줄 요약이 신규 intent 모드를 반영하지 않았고 `<branch>` 표기가 실제 `<slug>` 폴더 이름과 어긋나 있어 한 줄을 갱신함. match-image 테스트 부재는 기존 상태 유지(별도 인프라 작업 필요).