---
project: "claude-marketplace"
date: "2026-08-11"
summary: "appstore-assets 커밋 2건 감사 — tests/ 컨벤션 문서 보완 및 스크립트 목록 동기화 테스트 추가"
tags: ["docs", "tests"]
commits: 2
docsUpdated: 1
specsUpdated: 0
testsUpdated: 1
---

## 오늘의 변경 사항

- `6cafe48` test(appstore-assets): 셸 스크립트용 pytest 스위트 추가, tests/ 컨벤션 문서화
- `b13d84a` fix(appstore-assets): framer 앞쪽 스크린샷 x 오프셋을 445에서 370으로 조정 (중앙 정렬 구도)

## 문서 감사 결과

- `skills/CLAUDE.md` 업데이트 — 새로 도입된 `tests/` 컨벤션(런타임 코드를 가진 스킬은 pytest 스위트를 두고, 테스트는 저장소 전용으로 배포하지 않음)이 스킬 작성 규칙 문서에 빠져 있어서 Rules/Do 섹션에 한 줄 추가했어
- `docs/modules/skills.md`는 커밋 안에서 이미 함께 업데이트되어 최신 상태
- x 오프셋 변경(445→370)은 다른 문서 어디에도 옛 값이 남아 있지 않아 추가 수정 불필요
- 이 스킬을 다루는 별도 spec 파일은 존재하지 않음

## 테스트 감사 결과

- `skills/appstore-assets/tests/test_shell_scripts.py`에 `test_all_scripts_listed` 추가 — 하드코딩된 `ALL_SCRIPTS` 목록이 실제 `scripts/` 디렉토리의 `*.sh` 파일과 어긋나면 실패하도록 하는 드리프트 가드. 새 스크립트가 추가됐을 때 조용히 테스트에서 빠지는 걸 방지함
- 기존 스위트는 셸 스크립트 15개 전부를 커버하고 있었고(문법 스모크 + hermetic 스크립트 2개 기능 테스트), `frame_screenshots.py`도 전용 테스트 파일이 이미 존재
- x 오프셋 변경은 에이전트 프롬프트(마크다운) 내부의 레이아웃 값이라 실행 코드가 아니어서 별도 테스트 대상 아님
- 전체 스위트 실행 결과: **57개 테스트 모두 통과** (uv + pytest + pillow 환경)

## 요약

최근 커밋 2건은 모두 appstore-assets 스킬 관련이며 전반적으로 문서와 테스트가 잘 관리된 상태였어. 커밋에서 `docs/modules/skills.md`는 함께 갱신됐지만 스킬 작성 규칙의 원본인 `skills/CLAUDE.md`에는 tests/ 컨벤션이 누락되어 있어 보완했고, 테스트 스위트의 스크립트 목록이 하드코딩되어 있어 향후 드리프트를 막는 가드 테스트를 하나 추가했어. 소스 코드는 건드리지 않았고, 수정 후 전체 테스트 통과를 확인했어.